Custom radio button for single-select settings (study mode, exam track). Share a `name` to group.

```jsx
<Radio name="mode" label="Tutor mode" description="See explanations immediately" defaultChecked />
<Radio name="mode" label="Timed mode" description="Simulate exam conditions" />
```

For board-style A/B/C/D answer options use `Choice` instead.
