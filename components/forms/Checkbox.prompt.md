Custom checkbox with optional label + description — settings and multi-select filters.

```jsx
<Checkbox label="Unanswered only" defaultChecked />
<Checkbox label="Metabolic" description="42 questions available" />
```

Forwards native input props (`checked`, `defaultChecked`, `onChange`, `disabled`).
